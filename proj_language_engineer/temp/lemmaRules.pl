
% determiners exist

lemma(a,dtexists).

lemma(an,dtexists).

%lemma(there,dtexists).

lemma(some,dtexists).

 

% determiners

lemma(no,dtsp).

lemma(not,dtsp).

lemma(the,dtsp).

 

% determiners forall

lemma(each,dtforall).

lemma(all,dtforall).

lemma(every,dtforall).

 

% nouns

lemma(box,n).

lemma(container,n).

lemma(shelf,n).

lemma(bowl,n).

lemma(sandwich,n).

lemma(egg,n).

lemma(meat,n).

lemma(ham,n).

lemma(chicken,n).

lemma(milk,n).

lemma(almond,n).

lemma(popsicle,n).

lemma(banana,n).

lemma(watermelon,n).

lemma(fridge,n).

lemma(freezer,n).

 

 

% proper nouns

lemma(tom,pn).

lemma(sam,pn).

lemma(mia,pn).

lemma(sue,pn).

 

 

% WH pronouns

lemma(what,whprt).

lemma(which,whprt).

lemma(who,whprp).

 

% adjectives

lemma(red,adj).

lemma(white,adj).

lemma(blue,adj).

lemma(green,adj).

lemma(yellow,adj).

lemma(top,adj).

lemma(white,adj).

lemma(bottom,adj).

lemma(middle,adj).

lemma(empty,adj).

lemma(almond,adj).

 

% BE

lemma(is,betv).

lemma(is,be).

lemma(are,be).

lemma(will,be).

lemma(was,be).

lemma(were,be).

 

% verbs transitive

lemma(eat,tv).

%lemma(do,tv).

lemma(has,tv).

lemma(have,tv).

lemma(contain,tv).

lemma(drink,tv).

lemma(drank,tv).

lemma(belong,tv).

 

% verbs auxillary

lemma(do,aux).

lemma(does,aux).

lemma(did,aux).

lemma(will,aux).

 

% adjectives predicate

lemma(red,ap).

lemma(white,ap).

lemma(blue,ap).

lemma(green,adj).

lemma(yellow,ap).

lemma(white,ap).

lemma(empty,ap).

 

% verbs intransitive

lemma(drink,iv).

 

 

% ditransitive verb

lemma(put,dtv).

 

% adverbs

lemma(not,adv).

 

% prepositions

lemma(in,p).

lemma(at,p).

%lemma(to,p).

lemma(inside,p).

lemma(under,p).

lemma(of,p).

%lemma(on,p).

lemma(on,vacp).  

lemma(to,vacp).

 

% REL

lemma(that,rel).

lemma(this,rel).

lemma(these,rel).

lemma(those,rel).

lemma(who,rel).

lemma(what,rel).

lemma(which,rel).

 

% there

lemma(there,vacsp).

 

% numbers

lemma(one,one).

lemma(two,two).

lemma(three,three).

lemma(four,four).

lemma(five,five).

lemma(six,six).

lemma(seven,seven).

lemma(eight,eight).

lemma(nine,nine).

lemma(ten,ten).
